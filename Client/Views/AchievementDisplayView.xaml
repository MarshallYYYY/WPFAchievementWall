<UserControl
    x:Class="Client.Views.AchievementDisplayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Client.Views"
    xmlns:viewmodels="clr-namespace:Client.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:AchievementDisplayViewModel}"
    xmlns:advs="clr-namespace:Client.Views.AchievementDisplayViews"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <ScrollViewer
        Padding="20">
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!-- 左侧的成就记录时间线 -->
            <ItemsControl
                Grid.Column="0"
                ItemsSource="{Binding AllAchievement}">
                <ItemsControl.ItemTemplate>

                    <DataTemplate>
                        <advs:YearAchievementControl />
                    </DataTemplate>

                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!--右侧的具体内容展示-->
            <StackPanel
                Grid.Column="1"
                Margin="50,0,0,0"
                Background="Gray">
                <TextBlock
                    Text="fdadddfas"
                    FontSize="60" />
                <TextBlock
                    Text="fdadddfas"
                    FontSize="60" />
                <TextBlock
                    Text="fdadddfas"
                    FontSize="60" />
            </StackPanel>
        </Grid>

    </ScrollViewer>
</UserControl>
